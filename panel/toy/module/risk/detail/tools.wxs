var getWidth = function (time) {
  var t = time || 0;
  if (t < 1) t = 1;
  if (t > 20) t = 20;
  var minW = 130;
  var maxW = 280;
  var w = minW + ((t - 1) * (maxW - minW)) / 19;
  return w;
};
module.exports.getWidth = getWidth;
