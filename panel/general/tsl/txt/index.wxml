<view class="group">
  <form bindsubmit="formSubmit">
    <van-field model:value="{{ tslVal }}" size="large" type="textarea" placeholder="{{i18n['attrHolder']}}" border="{{ false }}" autosize clearable maxlength="{{maxlength}}" />
    <view class="btns">
      <view class="btnItem">
        <van-button custom-class="tslBtn" custom-style="{{'border-radius:'+skin['radius']}}" disabled="{{ tslVal.length > 0 ? false : true }}" block form-type="submit" color="{{ skin['primary'] }}">{{i18n['confirmBtn']}}</van-button>
      </view>
    </view>
  </form>
</view>
